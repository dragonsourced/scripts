
#!/bin/sh

case $1 in
    *)
        bspc node focused -t floating -l above
        xdotool getactivewindow windowsize 500 300
        mvwin bottom right
        bspc node focused -g sticky ;;
    undo)
        bspc node focused -g sticky -l normal -t tiling ;;
esac
